import { Inter, Lusitana, Belleza, Arimo } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const arimo = Arimo({ subsets: ['latin'] });

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const belleza = Belleza({
  weight: ['400'],
  subsets: ['latin'],
});